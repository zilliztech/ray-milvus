name: CI

on:
  push:
    branches: [main, master, dev]
  pull_request:
    branches: [main, master]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install lint tools
        run: pip install flake8 black isort

      - name: Check formatting with black
        run: black --check ray_milvus/ tests/

      - name: Check imports with isort
        run: isort --check-only ray_milvus/ tests/

      - name: Lint with flake8
        run: flake8 ray_milvus/ tests/ --max-line-length=120

  test-linux:
    name: Test Linux (Python ${{ matrix.python-version }})
    runs-on: ubuntu-22.04
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11"]  # Conan 1.x not compatible with Python 3.12

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Get milvus-storage commit ID
        id: milvus-storage
        run: |
          echo "commit=$(git -C milvus-storage rev-parse HEAD)" >> $GITHUB_OUTPUT

      # Cache native library build artifacts
      - name: Cache native library build
        id: cache-native
        uses: actions/cache@v4
        with:
          path: |
            milvus-storage/cpp/build
            milvus-storage/python/milvus_storage/lib
          key: native-linux-${{ steps.milvus-storage.outputs.commit }}

      # Only restore Conan/ccache when native cache misses
      - name: Cache Conan packages
        if: steps.cache-native.outputs.cache-hit != 'true'
        uses: actions/cache@v4
        with:
          path: ~/.conan
          key: conan-linux-${{ steps.milvus-storage.outputs.commit }}
          restore-keys: |
            conan-linux-

      - name: Cache ccache
        if: steps.cache-native.outputs.cache-hit != 'true'
        uses: actions/cache@v4
        with:
          path: ~/.ccache
          key: ccache-linux-${{ steps.milvus-storage.outputs.commit }}
          restore-keys: |
            ccache-linux-

      - name: Install system dependencies
        if: steps.cache-native.outputs.cache-hit != 'true'
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential ccache libssl-dev pkg-config

      - name: Install CMake
        if: steps.cache-native.outputs.cache-hit != 'true'
        run: |
          wget -qO- "https://cmake.org/files/v3.27/cmake-3.27.5-linux-x86_64.tar.gz" | \
            sudo tar --strip-components=1 -xz -C /usr/local

      - name: Install Conan
        if: steps.cache-native.outputs.cache-hit != 'true'
        run: |
          pip install conan==1.61.0
          conan profile new default --detect || true
          conan profile update settings.compiler.libcxx=libstdc++11 default
          conan remote add milvus https://milvus01.jfrog.io/artifactory/api/conan/default-conan-local --insert || true

      - name: Build milvus-storage C++ library
        if: steps.cache-native.outputs.cache-hit != 'true'
        run: |
          cd milvus-storage/cpp
          make python-lib
          mkdir -p ../python/milvus_storage/lib
          cp build/Release/libmilvus-storage.so ../python/milvus_storage/lib/
        env:
          CCACHE_DIR: ~/.ccache

      - name: Install Python dependencies
        run: |
          pip install pytest pytest-cov pyarrow numpy cffi pandas
          pip install "ray[data]>=2.51.1"

      - name: Setup milvus_storage Python package
        run: pip install -e milvus-storage/python/

      - name: Install ray-milvus
        run: pip install -e .

      - name: Run tests
        run: pytest tests/ -v --cov=ray_milvus --cov-report=xml
        env:
          LD_LIBRARY_PATH: ${{ github.workspace }}/milvus-storage/python/milvus_storage/lib

      - name: Upload coverage
        uses: codecov/codecov-action@v4
        if: matrix.python-version == '3.10'
        with:
          file: ./coverage.xml
          fail_ci_if_error: false

  test-macos:
    name: Test macOS (Python ${{ matrix.python-version }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # Use macos-13 (Intel) as abseil 20220623.0 doesn't support ARM64 (SSE4.1 instructions)
          - os: macos-13
            python-version: "3.10"
          - os: macos-13
            python-version: "3.11"

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Get milvus-storage commit ID
        id: milvus-storage
        run: |
          echo "commit=$(git -C milvus-storage rev-parse HEAD)" >> $GITHUB_OUTPUT

      # Cache native library build artifacts
      - name: Cache native library build
        id: cache-native
        uses: actions/cache@v4
        with:
          path: |
            milvus-storage/cpp/build
            milvus-storage/python/milvus_storage/lib
          key: native-macos-${{ runner.arch }}-${{ steps.milvus-storage.outputs.commit }}

      # Only restore Conan/ccache when native cache misses
      - name: Cache Conan packages
        if: steps.cache-native.outputs.cache-hit != 'true'
        uses: actions/cache@v4
        with:
          path: ~/.conan
          key: conan-macos-${{ runner.arch }}-${{ steps.milvus-storage.outputs.commit }}
          restore-keys: |
            conan-macos-${{ runner.arch }}-

      - name: Cache ccache
        if: steps.cache-native.outputs.cache-hit != 'true'
        uses: actions/cache@v4
        with:
          path: ~/Library/Caches/ccache
          key: ccache-macos-${{ runner.arch }}-${{ steps.milvus-storage.outputs.commit }}
          restore-keys: |
            ccache-macos-${{ runner.arch }}-

      - name: Install system dependencies
        if: steps.cache-native.outputs.cache-hit != 'true'
        run: brew install cmake ccache

      - name: Install Conan
        if: steps.cache-native.outputs.cache-hit != 'true'
        run: |
          pip install conan==1.61.0
          conan profile new default --detect || true
          conan profile update settings.compiler.libcxx=libc++ default
          conan profile update settings.compiler.version=15 default  # Force version for Conan 1.x compatibility
          conan remote add milvus https://milvus01.jfrog.io/artifactory/api/conan/default-conan-local --insert || true

      - name: Build milvus-storage C++ library
        if: steps.cache-native.outputs.cache-hit != 'true'
        run: |
          cd milvus-storage/cpp
          WITH_AZURE=OFF make python-lib
          mkdir -p ../python/milvus_storage/lib
          cp build/Release/libmilvus-storage.dylib ../python/milvus_storage/lib/
        env:
          CCACHE_DIR: ~/Library/Caches/ccache

      - name: Install Python dependencies
        run: |
          pip install pytest pytest-cov pyarrow numpy cffi pandas
          pip install "ray[data]>=2.51.1"

      - name: Setup milvus_storage Python package
        run: pip install -e milvus-storage/python/

      - name: Install ray-milvus
        run: pip install -e .

      - name: Run tests
        run: pytest tests/ -v
        env:
          DYLD_LIBRARY_PATH: ${{ github.workspace }}/milvus-storage/python/milvus_storage/lib

  test-windows:
    name: Test Windows (Python ${{ matrix.python-version }})
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10"]

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install Python dependencies
        run: |
          pip install pytest pyarrow numpy pandas
          pip install "ray[data]>=2.51.1"

      - name: Install ray-milvus (without milvus_storage)
        run: pip install -e .

      - name: Run basic import test
        run: |
          python -c "from ray_milvus import MilvusDatasource, MilvusDatasink; print('Import successful')"
        continue-on-error: true
